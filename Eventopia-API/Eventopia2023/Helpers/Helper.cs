using Eventopia2023.api.Models.Request;
using Eventopia2023.api.Models.Response;

namespace Eventopia2023.api.Helpers
{

    public static class Helper
    {
       //public static bool checkLoginCondition(LoginRequest request, LoginResponse loginData)
       // {
       //     return loginData.Username.Equals(request.Email, StringComparison.OrdinalIgnoreCase) && loginData.password_hash.Equals(request.Password, StringComparison.OrdinalIgnoreCase);
       // }
    }
}
